import React from 'react'
import { storiesOf } from '@storybook/react'
import { Card } from '../src'

storiesOf('Card', module).add('卡片-食用指南', () => {
  const style_1 = `
    background-color: #d8ff00;
  `
  const style_2 = `
    background-color: #ffbebe;
  `
  return <div className="btn-demo"></div>
})
